
const Wrapper = props => {
    //props.children holds all the content you're passing 
    //between the opening and closing tag of your custom component
    return props.children;
}
export default Wrapper;